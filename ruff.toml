# Ruff configuration for LiteAttention project

[format]
# Allow multiple arguments on the same line when they fit
# This preserves intentional grouping of logically related parameters
skip-magic-trailing-comma = true

# Exclude files where we want to preserve manual formatting
exclude = ["hopper/_internal/flash_attn_interface.py"]

